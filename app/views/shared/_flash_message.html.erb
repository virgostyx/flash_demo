<%
  # This partial is now a wrapper for the FlashMessageComponent
  # to maintain backward compatibility with any direct partial calls.
  type ||= :info
  message ||= ""
  duration ||= 5000
  width_px ||= 448
%>

<%= render FlashMessageComponent.new(type: type, message: message, duration: duration, width_px: width_px) %>
